pub struct GeneratorByDuration {
    pub Druation: usize,
    pub RedisUrl: String,
    pub RedisVersion: String,
    pub KeyLen: usize,
    pub Loopstep: usize,
    pub EXPIRE: usize,
    pub LogOut: bool,
}